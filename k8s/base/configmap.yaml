apiVersion: v1
kind: ConfigMap
metadata:
  name: retail-risk-config
  namespace: retail-risk
data:
  CASSANDRA_CONTACT_POINTS: cassandra
  CASSANDRA_PORT: "9042"
  CASSANDRA_KEYSPACE: retail_risk
  JANUSGRAPH_GREMLIN_ENDPOINT: ws://janusgraph:8182/gremlin
  MINIO_ENDPOINT: http://minio:9000
  MINIO_BUCKET: retail-risk
  ICEBERG_CATALOG_TYPE: nessie
  ICEBERG_WAREHOUSE: s3://retail-risk/warehouse
  ICEBERG_CATALOG_NAME: default
  ICEBERG_NAMESPACE: risk
  TRINO_HOST: trino
  TRINO_PORT: "8080"
  TRINO_CATALOG: iceberg
  TRINO_SCHEMA: default
  TRINO_USER: risk-user
  VECTOR_INDEX_BUCKET_PATH: s3://retail-risk/indices
  MODEL_VERSION: v1
  RNG_SEED: "42"
